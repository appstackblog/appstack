Options -Indexes
# Always try maintenance page first
DirectoryIndex index.html index.php maintenance.html

RewriteEngine On

# Force root to index.html when it exists
RewriteCond %{REQUEST_URI} ^/$
RewriteCond %{DOCUMENT_ROOT}/index.html -f
RewriteRule ^$ /index.html [L]
